/**
 * I find it pretty dumb that there has to be a parallell set of components for
 * the dataset collection view simply because our API can't return consistent results.
 */
import DatasetCollectionPanel from "./DatasetCollectionPanel";
export default DatasetCollectionPanel;